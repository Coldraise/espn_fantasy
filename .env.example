# Compose-level settings, read by `docker compose` itself (not injected into
# the container). Copy to .env to override.
#
# Host port to publish the site on. 8080 is a busy default on many machines --
# change it here rather than editing docker-compose.yml.
HOST_PORT=8080
